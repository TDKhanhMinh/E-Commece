package project.back_end.entity.product;

public enum AttributeType {
    TEXT,   // Nhập tay (Vd: Tên chip)
    SELECT, // Chọn từ danh sách (Vd: RAM 8GB, 16GB)
    NUMBER  // Nhập số (Vd: Kích thước màn hình)
}
